# RAG Introduction

RAG means Retrieval-Augmented Generation.

In a RAG workflow, the system first retrieves relevant content from a knowledge
base, then passes that context to a large language model so the model can answer
with project-specific or document-grounded information.

This approach is useful when the answer should be based on local documents
rather than only on the model's built-in knowledge.
